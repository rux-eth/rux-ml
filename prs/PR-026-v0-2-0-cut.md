# PR-026: v0.2.0 version cut

**Landed-in:** (not yet landed)

## Before Implementation (NON-NEGOTIABLE)

This PR MUST NOT be implemented until `PROCEDURE-pr-research.md` has been completed in full and its output appended to the `## Research findings` section below.

**Tier-1 PRs** (research-backed at design time): Phase 1 (State Assessment) is required to catch drift. Phases 2-4 may be light if no drift is found.

**Tier-2 PRs** (research-pending): all 5 phases of `PROCEDURE-pr-research.md` must run before this PR is written in final form.

**This PR is Tier-2** per the precedent of PR-021 (the v0.1.0 cut). The version-cut ritual is research-backed by PR-016 + PR-021; this PR adapts to the v0.1 → v0.2 wrinkle. All 5 phases of `PROCEDURE-pr-research.md` must run; Phases 2-4 should be light if no drift surfaces relative to PR-021.

Skipping the PR research procedure is a hard violation of the research-backed-decisions constraint in `docs/CONSTRAINTS.md`.

## Research findings

**Pattern locked** by PR-021 (the v0.0 → v0.1 analog). The `scripts/rewrite_doc_refs.py` script handles versioned→versioned migration via `PATH_REWRITES` + `OPT_OUT_GLOBS` + per-line `<!-- rewrite-doc-refs:skip-line -->` sentinel. PR-026 adapts: `PATH_REWRITES` becomes `docs/0.1/* → docs/0.2/*`; any new historical citations added since PR-021 (in CLAUDE.md / README.md / docs/SSOT/*) need the sentinel applied during dry-run review.

### Phase 1 — State Assessment (2026-05-19)

**Current state** (HEAD = `8eb4d91`, post-PR-027 merge on `dev`; PR-026 branch `pr-026/v0-2-0-cut` cut from there):

- **CHANGELOG `[Unreleased]` contents** (17 entries):
  - `### Fixed` (4): PR-027 Int64-timestamp; PR-022 base.toml discriminator hygiene; PR-022 HPO docs reframe; PR-023 stacked-panel under-embargo.
  - `### Added` (10): PR-022 base.toml hygiene CONVENTIONS subsection; PR-022 gap row-vs-time subsection; PR-023 PanelCombinatorialPurgedCV variant; PR-023 ergonomic AFML knobs; PR-023 ARCHITECTURE Decision Rules entry; PR-023 CONVENTIONS CV-strategy + skfolio purge note; PR-023 20 new panel-CV tests; PR-024 temporal_train_val_test_split + dispatch; PR-024 ARCHITECTURE one-off-baseline cross-ref; PR-024 CONVENTIONS one-shot subsection; PR-024 16 new tests.
  - `### Changed` (3): PR-023 TimeSeriesSplitCV polymorphic embargo; PR-024 cli/train + registry/promote dispatcher route; **PR-025 docs HPO objective sections quantify Position A bias (NUMERIC DRIFT — see below)**.
- **CHANGELOG split target**:
  - **`[0.1.1]`**: PR-022 only (4 entries: 2 Fixed + 2 Added). The v0.1.1 PATCH that never cut its own tag.
  - **`[0.2.0]`**: PR-023 + PR-024 + PR-025 + PR-027 (13 entries: 2 Fixed + 8 Added + 3 Changed).
- **PR-025 numeric drift surfaced during 2026-05-19 empirical validation** (against `prs/PR-025-calibration-results.json` JSON receipt):
  - CHANGELOG claim: "B median 0.027101 (identical to 6 decimals)" → JSON receipt: **B median 0.027108** (identical to A only at 4 decimals; diff = 7e-06, still within Optuna search noise).
  - CHANGELOG claim: "C +0.26%" → JSON receipt: **C +0.42%** (0.027215 vs A 0.027101).
  - CHANGELOG claim: "60% slower wallclock" → JSON receipt: **+65.2%** (C 490.4s vs A 296.8s).
  - CHANGELOG omits: **B wallclock = −29.2% (B is the fastest position**, 210.0s vs A 296.8s; B saves one XGBoost evaluation per fold).
  - **PR-026 corrects these four numerics in the `[0.2.0] ### Changed` entry** before publishing the version cut. Qualitative conclusion ("stay on A; A ≡ B within search noise; C strictly worse") still holds.
- **`Landed-in:` header backfill scope**:
  - `prs/PR-022-*.md`: `(not yet landed)` → **`v0.1.1`**.
  - `prs/PR-023-*.md`: `v0.2.0 (rolled in PR-026)` → plain **`v0.2.0`** (PR-021 precedent on tightening once the cut lands).
  - `prs/PR-024-*.md`: `v0.2.0 (rolled in PR-026)` → plain **`v0.2.0`**.
  - `prs/PR-025-*.md`: `v0.2.0 (rolled in PR-026)` → plain **`v0.2.0`**.
  - `prs/PR-027-*.md`: `(not yet landed)` → **`v0.2.0`**.
  - `prs/PR-026-*.md`: stays **`(not yet landed)`** per PR-021 self-reference limitation.
- **`scripts/rewrite_doc_refs.py` current state**:
  - PATH_REWRITES still at `docs/0.0/* → docs/0.1/*` (PR-021 mapping). PR-026 swap: **`docs/0.1/* → docs/0.2/*`** for the three temporal docs.
  - OPT_OUT_GLOBS includes `docs/0.0/**` and `docs/0.1/**` but **NOT `docs/0.2/**`** — must be added (per the dry-run mis-rewrite I caught at session start: the script wanted to rewrite 4 lines in `docs/0.2/{DESIGN-log,RESEARCH-BACKLOG,ROADMAP}.md` from `docs/0.0` → `docs/0.1`, which is wrong because those are historical citations to v0.0 era inside v0.2 frozen docs).
  - Sentinel pattern unchanged (PR-021 codified).
- **Mixed-file `docs/0.1/` references that PR-026's rewrite must process** (16 lines across 9 files):
  - **Active forward-pointers** (auto-rewrite to `docs/0.2/*`; correct behavior):
    - `README.md:648, 689, 690, 691` — Design References table rows pointing at the current-version docs.
    - `docs/CONSTRAINTS.md:51` — "tracks which decisions have research" (navigation to active DESIGN-log).
    - `docs/DEPLOYMENT.md:60` — "check the watch note in `docs/0.1/RESEARCH-BACKLOG.md` Drift Watch" (active backlog pointer).
    - `PROCEDURE-pr-research.md:23, 132, 165, 198` — references to active DESIGN-log / ROADMAP / RESEARCH-BACKLOG.
    - `PROCEDURE-design-planning.md:41, 74, 75, 76` — references to active design + roadmap + backlog.
    - `PROCEDURE-code-audit.md:51` — "Update `docs/0.1/ROADMAP.md`" (active backlog navigation).
  - **Historical citations** (require `<!-- rewrite-doc-refs:skip-line -->` sentinel; lock pinned at `docs/0.1/`):
    - `docs/VERSIONING.md:19` — "per PR-017 / `docs/0.1/DESIGN-log.md` Q6" (specific PR-017 design decision from v0.1 era).
    - `docs/ARCHITECTURE.md:251, 253, 298` — "per PR-020 / `docs/0.1/DESIGN-log.md` Q1", "Per `docs/0.1/DESIGN-log.md` Q1", "per PR-017 / `docs/0.1/DESIGN-log.md` Q2" (specific v0.1 design decisions).
    - `docs/CONVENTIONS.md:375, 438` — "Per `docs/0.1/DESIGN-log.md` Q1", "Per `docs/0.1/DESIGN-log.md` Q1–Q5 + Q6" (specific v0.1 design decisions).
  - **8 lines auto-rewrite, 7 lines sentinel** — close to PR-021's tally (29 replacements across 9 mixed files, 7 historical-citation pins).
- **`pyproject.toml` current version**: `0.1.0` → bump to **`0.2.0`** (single integer string edit; `__version__` derives via `importlib.metadata`, no `src/rux_ml/__init__.py` edit needed per PR-021 precedent).
- **`docs/0.2/ROADMAP.md` state**: 4 of 5 v0.2-era rows are `[x]` (PR-022, PR-023, PR-024, PR-025, PR-027); PR-026 row is `[ ]` — flip in the implementation commit per `feedback_roadmap_flip_in_pr`.
- **`docs/0.2/RESEARCH-BACKLOG.md` state**: PR-026 row already documents the CHANGELOG numeric-correction line item (added at PR-027 setup time). PR-026's implementation marks it `fully-researched 2026-05-19` + `implementation-cleared 2026-05-19`.

**No drift since v0.1.0 cut precedent** (PR-021 at `cdbffd2`): the version-cut workflow + sentinel pattern + rewrite-script primitives are unchanged. PR-026 is mechanically the same shape as PR-021, with the version numbers updated.

**Stale assumptions in PR draft**:

- Stub Q3 left v0.1.1 cut decision open. **Phase 1 lean (intuition, flagged per `feedback_research_discipline`)**: cut both tags at this PR — `v0.1.1` retroactively at `027c3b0` (PR-022 merge commit) AND `v0.2.0` at PR-026's merge commit. The pyproject inconsistency at `027c3b0` (still `0.1.0` there, never bumped to `0.1.1`) is a documented historical quirk; PR-022 was largely docs + a structural test + a single `configs/base.toml` line removal. Decision deferred to Phase 4 user pick.
- Stub Q4 names PR-022/23/24/25 only — should also include PR-027 (`(not yet landed)` → `v0.2.0`). Updated above.

**New constraints** (from PR-027 just-merged on dev):

- PR-027 touched `src/rux_ml/data/cv.py`, `src/rux_ml/config/cv.py`, `tests/data/test_panel_cv.py`, `docs/CONVENTIONS.md`, `CHANGELOG.md`, `docs/0.2/ROADMAP.md`, `docs/0.2/RESEARCH-BACKLOG.md`. None of these are in PR-026's surface (rewrite-script, pyproject, CHANGELOG, Landed-in: headers, ROADMAP/RESEARCH-BACKLOG flips). **No conflict.**

**Exit decision**: zero drift relative to PR-021 precedent; richer-than-expected surface (PR-025 numeric corrections + PR-027 backfill row + `docs/0.2/**` OPT_OUT_GLOBS addition). All adaptations are mechanical extensions of PR-021's pattern; no design-cycle work. **Proceed to Phase 2 — light scope sharpening; no Phase 3 web research needed (PR-021 archived all the research).**

### Phase 2 — Scope Research (2026-05-19, light)

Per `feedback_phase1_scope`: Tier-2 with pattern locked by PR-021 → light Phase 2 (no external research). Sub-decisions resolved by user acknowledgement at 2026-05-19 Phase 1 approval.

**Resolved by user acknowledgement (2026-05-19 "approved" reply)**:

1. **CHANGELOG split** — **`[0.1.1]`** for PR-022's 4 entries (Fixed: base.toml hygiene + HPO docs reframe; Added: base.toml hygiene CONVENTIONS + gap row-vs-time subsection); **`[0.2.0]`** for PR-023/24/25/27's 13 entries.
2. **PR-025 numeric corrections** — `[0.2.0] ### Changed` entry for PR-025 updates to JSON-receipt-accurate values:
   - B median: `0.027101 (identical to 6 decimals)` → **`0.027108 (identical to A at 4 decimals; +7e-06, within Optuna search noise)`**.
   - C vs A: `(+0.26%)` → **`(+0.42%)`**.
   - C wallclock: `60% slower` → **`+65.2% slower`**.
   - B wallclock: omitted → **add `−29.2% (B is the fastest position; skips one XGBoost evaluation per fold)`**.
   - Qualitative conclusion (stay on A; A ≡ B within search noise; C strictly worse) unchanged.
3. **v0.1.1 tag retroactive at `027c3b0`** — accepted BEST-GUESS. Post-merge tagging sequence (documented in PR body for user to execute):
   - `git tag -a v0.1.1 027c3b0 -m "v0.1.1 — workbench correctness hygiene PATCH"` (the PR-022 merge commit).
   - `git tag -a v0.2.0 <PR-026 merge commit on dev> -m "v0.2.0 — time-aware CV for panel data + temporal split + eval_set calibration + Int64 timestamp fix"`.
   - `git push origin v0.1.1 v0.2.0`.
   - Documented inconsistency: `pyproject.toml` at `027c3b0` reads `version = "0.1.0"`, not `0.1.1`. v0.1.1 was a PATCH that conceptually shipped at PR-022's merge (`configs/base.toml` discriminator-table fix + docs reframe + structural test) but its pyproject bump was deferred to the v0.2.0 cut. Documented in the PR body's "Notes" section so future readers find the reasoning.
4. **Sentinel-application set** — 7 lines across 4 files need `<!-- rewrite-doc-refs:skip-line -->` after the rewrite script applies:
   - `docs/VERSIONING.md:19` — "per PR-017 / `docs/0.1/DESIGN-log.md` Q6".
   - `docs/ARCHITECTURE.md:251, 253, 298` — 3 references to v0.1 Q1/Q2 historical decisions.
   - `docs/CONVENTIONS.md:375, 438` — 2 references to v0.1 Q1 / Q1–Q5 + Q6.

   Other `docs/0.1/` references (README.md:648/689/690/691 + docs/CONSTRAINTS.md:51 + docs/DEPLOYMENT.md:60 + PROCEDURE-*.md ×9 lines) are **active navigation pointers** → auto-rewrite to `docs/0.2/*` (correct behavior).

**Must-answer** (resolved by Phase 1 + user approval; no Phase 3 dispatch):

- Q1: rewrite-script PATH_REWRITES + OPT_OUT_GLOBS swap → mechanical (PR-021 pattern + new `docs/0.2/**` row).
- Q2: historical-citation sentinel review → 7 lines identified above.
- Q3: CHANGELOG roll → resolved (split into `[0.1.1]` + `[0.2.0]` per #1 above).
- Q4: `Landed-in:` backfill → resolved at Phase 1 (PR-022 → `v0.1.1`; PR-023/24/25/27 → plain `v0.2.0`; PR-026 stays `(not yet landed)`).
- Q5: Post-merge tag command → resolved per #3 above.

**Phase 3 dispatch plan**: **SKIP.** Pattern locked by PR-021; all sub-decisions resolved by Phase 1 evidence + user acknowledgement. Proceed to Phase 4 directly per `feedback_phase1_scope` ("Tier-2 with research archived → straight to Synthesis").

**Exit decision**: full scope locked. Proceed to Phase 4 Synthesis (one-line confirmation; no new design content) then Phase 5 implementation.

### Phase 4 — Synthesis (2026-05-19)

**Outcome**: **Confirm with locked specifics**. No research re-dispatch needed; all decisions inherit PR-021 + user-approved leans from Phase 1.

**Final implementation shape** (locked):

1. **`scripts/rewrite_doc_refs.py`**:
   - PATH_REWRITES: 3 rows, `docs/0.1/* → docs/0.2/*` for DESIGN-log.md / RESEARCH-BACKLOG.md / ROADMAP.md.
   - OPT_OUT_GLOBS: add `"docs/0.2/**"` (keep `docs/0.0/**`, `docs/0.1/**` for cross-cut idempotency).
   - Module docstring + run-order comments updated to reference the v0.1 → v0.2 cut.
2. **Rewrite-script execution**:
   - `make rewrite-doc-refs-dry` → review expected ~8 auto-rewrite changes.
   - `make rewrite-doc-refs` → apply.
   - Manually apply 7 sentinels to historical citations (Phase 2 #4 list).
   - `make rewrite-doc-refs-dry` again → must report 0 changes (idempotency).
3. **`pyproject.toml`** — single edit: `version = "0.1.0"` → `version = "0.2.0"`.
4. **`CHANGELOG.md`** — rewrite `[Unreleased]` into three sections:
   - Fresh empty `[Unreleased]`.
   - `[0.2.0] - 2026-05-19` with PR-023 + PR-024 + PR-025 (corrected) + PR-027 entries.
   - `[0.1.1] - 2026-05-18` with PR-022 entries (using `027c3b0` merge date).
   - Add `[0.2.0]` + `[0.1.1]` compare-link refs at the bottom (per Keep-a-Changelog 1.1.0).
5. **`Landed-in:` flips**:
   - `prs/PR-022-*.md`: `(not yet landed)` → `v0.1.1`.
   - `prs/PR-023-*.md` / `prs/PR-024-*.md` / `prs/PR-025-*.md`: `v0.2.0 (rolled in PR-026)` → `v0.2.0`.
   - `prs/PR-027-*.md`: `(not yet landed)` → `v0.2.0`.
   - `prs/PR-026-*.md`: stays `(not yet landed)`.
6. **`docs/0.2/ROADMAP.md`** — PR-026 row `[ ]` → `[x]`.
7. **`docs/0.2/RESEARCH-BACKLOG.md`** — PR-026 row marked `fully-researched 2026-05-19` + `implementation-cleared 2026-05-19`.

**Changes to ARCHITECTURE.md / CONVENTIONS.md / CONSTRAINTS.md from research**: none (the SSOT docs are unchanged at semantic level — only `docs/0.1/*` references get rewritten to `docs/0.2/*` by the script per the migration step).

**New PRs that must come first**: none.

### Phase 5 — Gate Check (2026-05-19)

- Premise still valid: ✓ (v0.2 implementation PRs 022/23/24/25/27 all merged on `dev` at `8eb4d91`)
- No prerequisite PRs surfaced: ✓
- User approved updated spec: ✓ (Phase 1 + Phase 2 approval 2026-05-19)
- Implementation cleared: ✓ (pending pre-commit user approval per Per-Phase Approval Gate)

### Phase 5 — Implementation outcomes (2026-05-19)

Mini state-assessment: zero days elapsed since Phase 4; `git log` between PR-027 merge (`8eb4d91`) and PR-026 implementation start shows no commits — no drift.

**Code landed**:

- `scripts/rewrite_doc_refs.py` — PATH_REWRITES swapped to `docs/0.1/* → docs/0.2/*` (3 rows); OPT_OUT_GLOBS gains `"docs/0.2/**"`; module docstring + opt-out comment updated to reflect the v0.1 → v0.2 cut.
- **8 sentinels applied** (BEFORE running the rewriter, so the script skips them):
  - `CHANGELOG.md:36` — frozen [0.1.0] entry referencing PR-020 Solver layer's v0.1 DESIGN-log Q1 citation.
  - `docs/VERSIONING.md:19` — PR-017 family-removal policy citation.
  - `docs/ARCHITECTURE.md:251, 253, 298` — 3 references to v0.1 Q1/Q2 historical decisions.
  - `docs/CONVENTIONS.md:375, 438` — 2 references to v0.1 Q1 / Q1-Q5 + Q6.
- **Rewriter ran cleanly**: 16 active-navigation replacements across 6 files (PROCEDURE-code-audit.md ×1; PROCEDURE-design-planning.md ×4; PROCEDURE-pr-research.md ×4; README.md ×5; docs/CONSTRAINTS.md ×1; docs/DEPLOYMENT.md ×1). Re-dry-run reports 0 changes (idempotency preserved).
- `pyproject.toml` — `version = "0.1.0"` → `"0.2.0"`. `importlib.metadata.version("rux-ml")` reports `0.2.0` post-bump.
- `CHANGELOG.md` — `[Unreleased]` split into:
  - Fresh empty `[Unreleased]`.
  - **`[0.2.0] - 2026-05-19`** with 2 Fixed + 8 Added + 3 Changed entries (PR-023 + PR-024 + PR-025-corrected + PR-027).
  - **`[0.1.1] - 2026-05-18`** with 2 Fixed + 2 Added entries (PR-022 only; date is the PR-022 merge commit `027c3b0`).
  - Compare-link refs at the bottom: `[Unreleased]` → `v0.2.0...HEAD`; new `[0.2.0]` → `v0.1.1...v0.2.0`; new `[0.1.1]` → `v0.1.0...v0.1.1`.
- **PR-025 numeric corrections applied** to the `[0.2.0] ### Changed` entry (per Phase 1 surfacing of the 4 numerical drifts vs `prs/PR-025-calibration-results.json` JSON receipt):
  - B median: `0.027101 (identical to 6 decimals)` → `0.027108 (identical to A at 4 decimals; diff = 7e-06, well within Optuna search noise)`.
  - C vs A: `(+0.26%, 60% slower)` → `(+0.42%, +65% slower)`.
  - B wallclock added: `−29% vs A (B is the fastest position; skips one XGBoost evaluation per fold)`.
  - Qualitative conclusion ("stay on A; A ≡ B within search noise; C strictly worse") unchanged.
- **`Landed-in:` flips**:
  - `prs/PR-022-*.md`: `(not yet landed)` → **`v0.1.1`**.
  - `prs/PR-023-*.md` / `prs/PR-024-*.md` / `prs/PR-025-*.md`: `v0.2.0 (rolled in PR-026)` → plain **`v0.2.0`**.
  - `prs/PR-027-*.md`: `(not yet landed)` → **`v0.2.0`**.
  - `prs/PR-026-*.md`: stays **`(not yet landed)`** per PR-021 self-reference limitation.
- `docs/0.2/ROADMAP.md` PR-026 row flipped `[ ]` → `[x]` per `feedback_roadmap_flip_in_pr`. Row description updated to reflect the v0.1.1 + v0.2.0 dual-tag plan.
- `docs/0.2/RESEARCH-BACKLOG.md` PR-026 row marked `fully-researched 2026-05-19` + `implementation-cleared 2026-05-19`.

**Test outcomes**:
- `uv run pytest` — **367 passed, 1 skipped, 15 deselected** (matches dev @ `8eb4d91`; PR-026 is docs-only so no test count delta).
- `uv run ruff check .` — clean.
- `uv run basedpyright src/` — clean (0 errors).
- `uv run python -c "from importlib.metadata import version; print(version('rux-ml'))"` → `0.2.0`.

**Post-merge tag sequence** (user executes after merging this PR into `dev`):

```bash
git tag -a v0.1.1 027c3b0 -m "v0.1.1 — workbench correctness hygiene PATCH"
git tag -a v0.2.0 <PR-026 merge commit on dev> -m "v0.2.0 — time-aware CV for panel data + temporal split + eval_set calibration + Int64 timestamp fix"
git push origin v0.1.1 v0.2.0
```

Verify post-tag:
- `git describe --tags` reports `v0.2.0`.
- `git tag --list | grep v0` shows `v0.0.1`, `v0.1.0`, `v0.1.1`, `v0.2.0`.

**Deferred / out of scope**:
- The v0.1.1 tag at `027c3b0` is a documented historical quirk: `pyproject.toml` at that commit reads `0.1.0`, never bumped to `0.1.1`. v0.1.1 was a PATCH that conceptually shipped at PR-022's merge (`configs/base.toml` discriminator fix + structural test + HPO docs reframe) but its pyproject bump was deferred to this v0.2.0 cut. Accepted at Phase 1 as the cleaner option vs losing the PATCH semantic.

**Open research questions** (must be resolved at Phase 1 state assessment):

1. **PATH_REWRITES + OPT_OUT_GLOBS migration.** Update the script's mapping table from `docs/0.0/* → docs/0.1/*` to `docs/0.1/* → docs/0.2/*`. Verify OPT_OUT_GLOBS still excludes the right paths (prs/, docs/0.0/, docs/0.1/, docs/0.2/, src/, tests/). Confirm idempotency post-apply.
2. **Historical-citation review.** Manually inspect mixed files (CLAUDE.md, README.md, docs/SSOT/*) post-rewrite-dry-run. Apply `<!-- rewrite-doc-refs:skip-line -->` sentinel to any references that should stay pinned at v0.1 (analog of PR-021's D1-D17 / D14 / D15 / dated-session pins).
3. **CHANGELOG roll.** PR-022's `[Unreleased]` entries need to roll into `[0.1.1]`. PR-023/024/025's `[Unreleased]` entries (after merging) roll into `[0.2.0]`. **Open**: cut v0.1.1 separately first (`v0.1.1` tag at the PR-022 merge commit `027c3b0`) and THEN cut v0.2.0, or roll both at v0.2.0? Decide at Phase 4 based on the state of `[Unreleased]` at the time PR-026 lands.
4. **`Landed-in:` backfill scope.** Flip PR-023 / PR-024 / PR-025 `Landed-in: (not yet landed)` → `Landed-in: v0.2.0`. Flip PR-022 from `(not yet landed)` → `v0.1.1` (or `v0.2.0` if v0.1.1 isn't cut separately). PR-026 itself stays `(not yet landed)` per the self-reference limitation (PR-021 precedent).
5. **Post-merge tag command.** Analogous to PR-021's `git tag -a v0.2.0 -m "..."` ritual. Document in PR body.

## Scope

Cut version `v0.2.0` per `docs/VERSIONING.md §2` workflow step 6. This is the final v0.2 PR; ships after PR-023 / PR-024 / PR-025 are merged on `dev`.

Concretely:

- **Update `scripts/rewrite_doc_refs.py`**:
  - Change `PATH_REWRITES` to `[("docs/0.1/DESIGN-log.md", "docs/0.2/DESIGN-log.md"), ...]` for the v0.2 cut.
  - Update `OPT_OUT_GLOBS` if any new path patterns surfaced (e.g., new test-file globs).
  - Keep idempotency contract (re-run reports 0 changes).
- **Run the script**:
  - `make rewrite-doc-refs-dry` → review diff.
  - Manually inspect mixed files; apply sentinel where needed.
  - `make rewrite-doc-refs` → apply.
  - Re-run `make rewrite-doc-refs-dry` → verify 0 changes (idempotency).
- **Version bumps**:
  - `pyproject.toml` `version = "0.2.0"` (was `0.1.0`).
  - `__version__` auto-derives via `importlib.metadata`.
- **CHANGELOG rewrite**:
  - Rename `[Unreleased]` to `[0.2.0] - YYYY-MM-DD`.
  - Open fresh `[Unreleased]`.
  - Add comparison link refs at bottom: `[0.2.0]: https://github.com/rux-eth/rux-ml/compare/v0.1.X...v0.2.0`.
  - Decide v0.1.1 split per Q3 above.
- **Flip `Landed-in:` headers** in `prs/PR-022-*.md` through `prs/PR-025-*.md` per Q4.
- **Update `CLAUDE.md` Design References** — re-point versioned-doc references from `docs/0.1/*` to `docs/0.2/*` (rewrite script handles mechanically).
- **`docs/0.2/ROADMAP.md` flip** — PR-026 row `[ ]` → `[x]` (per memory `feedback_roadmap_flip_in_pr`).
- **`docs/0.2/RESEARCH-BACKLOG.md`** — PR-026 row marked `fully-researched` + `implementation-cleared`.
- **Post-merge tag command** documented for user (Q5).

Out of scope: any code changes to `src/rux_ml/` beyond `__version__` derivation. Any test changes. Any implementation work — this PR is purely the version-cut ritual.

## Dependencies

**PR-023, PR-024, PR-025** — all must be merged first. PR-026 lands last.

## Architecture section implemented

None — this PR is the version-cut ritual, not an architecture change.

`docs/VERSIONING.md` §2 (minor cut workflow) + §5 (cross-reference migration) + §7 (tag and deploy alignment) — exercises the workflow established at v0.1.0 cut.

## Verification criteria

Populated after research. Initial sketch:

- [ ] `scripts/rewrite_doc_refs.py` PATH_REWRITES updated for `docs/0.1/* → docs/0.2/*`.
- [ ] `make rewrite-doc-refs-dry` after apply reports 0 changes (idempotency preserved).
- [ ] Historical citations re-pinned via sentinel where needed.
- [ ] `pyproject.toml` `version = "0.2.0"`.
- [ ] `uv run python -c "import rux_ml; print(rux_ml.__version__)"` → `0.2.0`.
- [ ] `CHANGELOG.md` carries `[0.2.0] - YYYY-MM-DD` with consolidated v0.2 entries.
- [ ] `CHANGELOG.md` has fresh `[Unreleased]` section.
- [ ] `prs/PR-022-*.md` through `prs/PR-025-*.md` carry appropriate `Landed-in:` values.
- [ ] `CLAUDE.md` Design References point at `docs/0.2/*`.
- [ ] `docs/0.2/ROADMAP.md` PR-026 row flipped to `[x]`.
- [ ] `docs/0.2/RESEARCH-BACKLOG.md` PR-026 row marked `fully-researched` + `implementation-cleared`.
- [ ] Existing tests + typecheck + lint stay green (`make test` + `uv run basedpyright src/` + `uv run ruff check .`).
- [ ] Post-merge tag command documented in PR description.

## Research backing

Version-cut workflow locked by PR-016 + PR-021. PR-026 adapts the same workflow for the v0.1 → v0.2 cut.

Reference: `prs/PR-021-v0-1-0-cut.md` for the precedent.

## Notes

- This is the **version-cut ritual** PR. Per `docs/VERSIONING.md §2` step 6, lands last + immediately precedes the `v0.2.0` git tag.
- `Landed-in:` flip for PR-026 itself stays at `(not yet landed)` per the PR-021 self-reference limitation. Per memory `feedback_pr_spec_historicity`, this is acceptable.
- Post-merge, update memory `project_v0_rollout_state` to reflect the v0.2.0 cut (and the 26 PRs merged total: PR-001..PR-026).
- **v0.1.1 PATCH cut decision** is open: cut separately first (gives PR-022 its own version) or roll into v0.2.0 (simpler but loses the PATCH semantic). Decide at Phase 4 based on whether any v0.1.1-bound work remains.
