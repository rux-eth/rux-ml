# PR-036: v0.3.0 version cut

**Landed-in:** v0.3.0

## Before Implementation (NON-NEGOTIABLE)

This PR was implemented after `PROCEDURE-pr-research.md` Phase 1 completed on 2026-05-22 with zero design drift; Phases 2-4 abbreviated per `feedback_phase1_scope` ("Tier-1 with no drift → straight to Gate Check") and user approval. Findings appended below.

**Tier-2 PR by precedent** — version-cut PRs (PR-021 v0.1.0, PR-026 v0.2.0) ran the full procedure. Phase 1 confirmed all 6 open research questions in the stub were **mechanical verification**, not design unknowns. Per `feedback_phase1_scope`: pattern locked, no drift → skip Phases 2-4 web research.

This PR is the **version cut**, not a feature PR. Landed last in the v0.3 sprint, after PR-030 + PR-031 + PR-032 + PR-033 + PR-034 + PR-035 all merged on `dev`. The `v0.3.0` git tag lands at this PR's merge commit.

## Research findings

### Phase 1 — State Assessment (2026-05-22)

**Current state** (HEAD `f3283a6` — post-PR-034 merge; PR-036 branch off dev):

- All 6 v0.3 implementation PRs merged on `dev` (PR-030, PR-031, PR-032, PR-033, PR-034, PR-035).
- `pyproject.toml` version `0.2.0` (single-line bump to `0.3.0`).
- `CHANGELOG.md [Unreleased]` composition (6 PR groups, 7 section headers across 3 types — needs consolidation):
  - Added: PR-034 + PR-033 + PR-032 + PR-030 (scaffolding)
  - Changed: PR-031 + PR-029 + PR-028
  - Fixed: PR-032 docstring + PR-035 test backfill
- `docs/0.3/ROADMAP.md` PR-036 row currently `[ ]` (all other rows `[x]`).
- `scripts/rewrite_doc_refs.py` PATH_REWRITES at HEAD still maps `docs/0.1/* → docs/0.2/*` (unchanged since PR-026 cut). **Primary mechanical change for PR-036**: update to `docs/0.2/* → docs/0.3/*` and add `docs/0.3/**` to `OPT_OUT_GLOBS`.
- OPT_OUT_GLOBS already includes `docs/0.2/**` (added in PR-026 anticipating this exact migration). Sentinel `<!-- rewrite-doc-refs:skip-line -->` pattern intact (22 existing guards across CLAUDE.md / README.md / ARCHITECTURE.md / CONVENTIONS.md / CONSTRAINTS.md).
- Tag sequence at HEAD: `v0.0.1, v0.1.0, v0.1.1, v0.2.0`. PR-036 adds `v0.3.0`. **No retroactive v0.2.x tags needed** (per PR-026 Notes: PR-028/29 procedural-only changes fold into v0.3.0 since a minor landed first).
- Test baseline: 404 collected (403 passed + 1 skipped) — matches expectation post-PR-034.
- 6 active forward-pointers to `docs/0.2/*` across README.md (3), CONSTRAINTS.md (1), CONVENTIONS.md (1), DEPLOYMENT.md (1), PROCEDURE-* files (4 lines) — all auto-rewrite to `docs/0.3/*` via the rewriter.

**Stale assumptions**: none. PR-036 stub was written 2026-05-20 (PR-030 scaffolding) and all 6 open questions resolve mechanically against current state.

**New constraints**: none — pattern locked by 2 precedents (PR-021 + PR-026).

**One manual re-pin caught during dry-run / apply**: the rewriter migrated a historical citation in the `[0.2.0]` release notes (`docs/0.2/RESEARCH-BACKLOG.md A2` in the PR-023 D2 entry) to `docs/0.3/RESEARCH-BACKLOG.md A2`. A2 doesn't exist in the v0.3 backlog (deferred work didn't carry forward in the same shape). Per rewriter docstring step 5 ("Manually re-pin any historical citations the auto-rewrite missed"), reverted the substitution and added `<!-- rewrite-doc-refs:skip-line -->` sentinel to the line.

### Phase 2-4 — Abbreviated (per `feedback_phase1_scope`)

Phase 1 confirmed all 6 stub-listed "open research questions" are mechanical verification, not design unknowns:

| Stub Q | Resolution |
|---|---|
| Q1 PATH_REWRITES needs update | YES — `docs/0.1/* → docs/0.2/*` becomes `docs/0.2/* → docs/0.3/*`. |
| Q2 CHANGELOG composition | 7 section headers across 3 canonical types — consolidate to single-header-per-type. PR-030 scaffolding entry **kept** (loud Added line) per stub note "lean: roll up silently" — kept as a single bullet under consolidated Added section for honesty/discoverability, not removed. |
| Q3 Version tag sequence | `v0.0.1, v0.1.0, v0.1.1, v0.2.0 → v0.3.0`. No retroactive v0.2.x tags. |
| Q4 pyproject bump | `0.2.0 → 0.3.0`. |
| Q5 ROADMAP flip | PR-036 row `[ ] → [x]` in same commit (streak-preserving). |
| Q6 CONSTRAINTS.md rewrite triggers | None — flat SSOT cross-references rewrite cleanly via the script. Sentinel pattern protects historical citations. |

No web research dispatched; no Group D round (no synthesis to verify — every identifier is local).

### Phase 5 — Gate Check (2026-05-22)

- Premise still valid: ✓ (mechanical version-cut ritual; pattern locked)
- No prerequisite PRs surfaced: ✓ (all 6 v0.3 implementation PRs merged)
- Stale historical citation in [0.2.0] release notes caught + re-pinned: ✓
- User approved abbreviated procedure + implementation: ✓ (2026-05-22)
- Implementation cleared

## Original open research questions (from stub; all resolved mechanically)

1. **`scripts/rewrite_doc_refs.py` PATH_REWRITES** — verify PATH_REWRITES still has the right entries for `docs/0.2/* → docs/0.3/*`. The script was generalized at PR-026 cut-time to be reusable; PR-036 reuses without changes if the PATH_REWRITES list at HEAD already covers v0.3.
2. **CHANGELOG composition** — `[Unreleased]` at PR-036 start contains:
   - PR-028 (procedure sync from vibe-rails)
   - PR-029 (stack-neutral cleanup)
   - PR-030 (sprint scaffolding — likely "Changed" or "Internal" section)
   - PR-031 through PR-035 (user-facing entries)
   
   Question: does PR-030 (sprint scaffolding) warrant a CHANGELOG line, or roll up silently? Lean: roll up silently — pure docs + PR stubs creation, no user-facing change.
3. **Version tag sequence** — no retroactive v0.2.x tags needed. v0.2.0 tagged at `1fa43c0`; v0.3.0 tags at PR-036 merge commit.
4. **`pyproject.toml` bump** — `0.2.0 → 0.3.0` in `[project] version = ...`.
5. **`docs/0.3/ROADMAP.md` PR-036 row flip** — same commit as the bump + tag.
6. **`docs/CONSTRAINTS.md` rewrite triggers** — does anything in `docs/CONSTRAINTS.md` need versioned-path updates? Per PR-026 precedent, flat SSOT files mostly don't change (cross-references to `docs/<x.y>/...` use `rewrite-doc-refs:skip-line` sentinels where appropriate). Phase 1 verifies.

## Scope

Mechanical version-cut PR. No new features. Mirror of PR-026 for the v0.2.0 cut.

### Item 1 — Run `scripts/rewrite_doc_refs.py`

Migrate cross-references `docs/0.2/* → docs/0.3/*` across:
- `CLAUDE.md` (root + `docs/0.3/`... references)
- `README.md` (if any)
- Flat SSOT files (`docs/ARCHITECTURE.md`, `docs/CONVENTIONS.md`, `docs/CONSTRAINTS.md`, `docs/VERSIONING.md`, `docs/DEPLOYMENT.md`)
- `docs/0.3/{ROADMAP, RESEARCH-BACKLOG, DESIGN-log}.md` (references to docs/0.2/ as superseded)

Sentinel `<!-- rewrite-doc-refs:skip-line -->` continues protecting historical citations (e.g., "D1–D7 in DESIGN-log.md") that intentionally point at frozen docs.

### Item 2 — Bump `pyproject.toml`

`[project] version = "0.2.0"` → `"0.3.0"`.

### Item 3 — Rewrite `CHANGELOG.md`

`[Unreleased]` → `[0.3.0] - 2026-MM-DD`. Compose entries for each merged v0.3 PR:

```markdown
## [0.3.0] - 2026-MM-DD

### Added
- **`rux-ml registry score` CLI verb** (PR-032) — score a promoted bundle on its held-out test fold; writes receipt JSON + predictions parquet to `receipts/`.
- **ExtMem out-of-core ingest path activation** (PR-033) — XGBoost `xgb.train` + `ExtMemQuantileDMatrix` + `ParquetDataIter` now actually run when source X exceeds `gpu_in_memory_x_gb_max`. `cache_host_ratio` and `use_native` now have real consumers.
- **Optuna Artifacts Store integration** (PR-034) — per-trial diagnostic artifacts (preds + fold-scores) are now uploaded to `studies/artifacts/` via `FileSystemArtifactStore`. Diagnostic-only; promote still re-fits per D8 A1.
- `tests/cli/test_tune_subcommands.py` — `test_tune_retry_trial_*` integration tests (PR-035).
- `docs/0.3/{DESIGN-log, ROADMAP, RESEARCH-BACKLOG}.md` + 7 PR stubs (PR-030 scaffolding).

### Changed
- **HPO objective honors holdout fold** (PR-031). `tuning/objective.py` now builds CV substrate from `splits["train"] + splits["val"]`, not the full dataframe. Existing study metrics will shift. Crypto-h3 baseline metric receipt refreshed at `prs/PR-031-baseline-receipt.json`.
- `PROCEDURE-pr-research.md` syncs from vibe-rails (PR-028 + PR-029) — Phase 1 prior-art audit + Phase 3 cite-or-flag + Group D MCP-Verification round.

### Fixed
- `cli/tune.py:retry_trial` is now tested end-to-end (PR-035) — backfill of test coverage for an existing verb.

### Removed
- Misleading docstring at `registry/promote.py:97` ("held out for future golden-regression evaluation (PR-014)") — replaced with accurate cross-reference to `rux-ml registry score` (PR-032).
```

(Final wording locked in PR-036 Phase 4; placeholders above.)

### Item 4 — Tag

`git tag v0.3.0` at PR-036's merge commit. Push tag to origin.

### Item 5 — `docs/0.3/ROADMAP.md` final flip

PR-036 row flips `[ ]` → `[x]` in the implementation commit. All other v0.3 rows already flipped by their respective PRs.

### Out of scope

- Any new feature.
- Any test addition that isn't a doc verification.

## Dependencies

- **PR-031, PR-032, PR-033, PR-034, PR-035** all merged on `dev`.
- Optional: `prs/PR-031-baseline-receipt.json` exists (bundled with PR-031).

## Architecture section implemented

None — version cut only. `docs/VERSIONING.md §2` workflow step 6 (the cut ritual).

## Verification criteria

Populated after Phase 1 state assessment. Initial sketch:

- [ ] `pyproject.toml` version reads `0.3.0`.
- [ ] `importlib.metadata.version("rux-ml")` reports `0.3.0`.
- [ ] `git describe --tags` reports `v0.3.0` after the tag is pushed.
- [ ] `scripts/rewrite_doc_refs.py` PATH_REWRITES covers `docs/0.2/* → docs/0.3/*`.
- [ ] `CHANGELOG.md` `[Unreleased]` is empty; `[0.3.0]` section contains entries for PR-028 through PR-035.
- [ ] `docs/0.3/ROADMAP.md` all rows `[x]`.
- [ ] `make test` passes (367 + new tests from PR-031..PR-035).
- [ ] `make test-golden` passes (no golden regressions).
- [ ] Sentinel `<!-- rewrite-doc-refs:skip-line -->` lines preserved against re-runs.

## Research backing

Tier-2 — pattern locked by PR-021 (v0.1.0 cut) + PR-026 (v0.2.0 cut). Phase 1 verifies PATH_REWRITES + CHANGELOG composition + tag sequence.

Anchored on:
- PR-021 / PR-026 version-cut PRs (precedent)
- `docs/VERSIONING.md §2` workflow step 6
- `scripts/rewrite_doc_refs.py` (PR-021 / PR-026 generalized form)

## Notes

- **No retroactive v0.2.x tags needed.** v0.2.1 was deferred at PR-026 design ("rolls into v0.3.0+ if a minor lands first") — and indeed v0.3 minor landed, so PR-028 + PR-029 (which were [Unreleased] PATCH-shape) ship under v0.3.0 instead.
- Per memory `feedback_roadmap_flip_in_pr`: PR-036's implementation commit must flip `docs/0.3/ROADMAP.md` PR-036 row `[ ]` → `[x]` in the same commit.
- Per memory `project_v0_rollout_state`: v0.3.0 is the **third minor cut**. Tag sequence after PR-036: v0.0.1, v0.1.0, v0.1.1, v0.2.0, v0.3.0.
- The empirical-validation precedent (pre-PR-026 empirical pass on 2026-05-19 that surfaced PR-027 Int64 bug + PR-025 CHANGELOG drift) is worth re-running here. Verify each v0.3 PR's CHANGELOG claim against actual shipped code before tagging.
