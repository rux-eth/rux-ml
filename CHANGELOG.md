# Changelog

All notable user-facing changes to `rux-ml`. Format: [Keep a Changelog 1.1.0](https://keepachangelog.com/en/1.1.0/). Versioning policy: see [`docs/VERSIONING.md`](docs/VERSIONING.md).

## [Unreleased]

### Changed
- **HPO objective honors the holdout fold** (PR-031). `tuning/objective.py:build_objective` now builds its CV substrate from `splits["train"] + splits["val"]` instead of the full source dataframe — the `splits["test"]` partition carved by `make_splits` (PR-024) is finally honored as a true held-out window unseen by HP search. The substrate carve is done once outside the per-trial closure via the new module-level helper `_carve_substrate(base_cfg, df_full, target_col)` using the study-level bag's `split_seed` (`make_seed_bag(master_entropy=cfg.tuning.entropy, trial_number=0).split_seed`) — same `trial_number=0` sentinel pattern as the existing sampler seed at `cli/tune.py:73` — so all trials in a study share the identical substrate (deterministic per study identity for both `random` and `time_ordered` `data.split_kind`). **This is a behavior change**: any existing study TOML will produce different metrics after this PR because CV now operates on 85% of the source data (default `data.split_ratios = {train: 0.7, val: 0.15, test: 0.15}`), not 100%. The crypto-h3 baseline at RMSE 0.027101 measured during PR-025 calibration is **not** a regression target — it was measured against the OLD 100%-substrate semantics. Fresh receipt at `prs/PR-031-baseline-receipt.json`. Variable rename inside `build_objective`: `x_full` → `x_substrate`, `y_full` → `y_substrate` for honesty. Convention citation (Phase 3 web research, 2026-05-21): AutoGluon `tabular-essentials.html` at SHA `f8c428cbbef3bc319ff3f7710f5900e65637f4c4` (`fit(train_data) → evaluate(test_data)`); sklearn user guide §3.1; Optuna issue #2184 (in-objective splitting acknowledged as anti-pattern). Two new behavior tests at `tests/tuning/test_objective.py`: `test_carve_substrate_excludes_test_fold_for_time_ordered` + `test_carve_substrate_random_is_deterministic_across_invocations`. `cli/train.py` and `registry/promote.py` unchanged — both already used `make_splits["train"] + ["val"]` per PR-024.

### Fixed
- **Test coverage backfill for `rux-ml tune retry-trial`** (PR-035). Three integration tests added to `tests/cli/test_tune_subcommands.py` covering the three distinct code paths in the verb body at `cli/tune.py:206-231`: `test_tune_retry_trial_enqueues_prior_params` (success — re-enqueues prior params via `study.enqueue_trial` + `study.optimize(n_trials=1)`); `test_tune_retry_trial_missing_trial_raises_bad_parameter` (missing trial → `BadParameter`); `test_tune_retry_trial_missing_study_raises_bad_parameter` (missing study → `BadParameter`). Closes the test-coverage gap surfaced by the 2026-05-20 phantom audit. The verb body is state-agnostic — it retries any trial regardless of state — so no failed-trial simulation is needed. Default test suite count moves from 367 → 370. No production code change.

### Added
- **v0.3 sprint scaffolding** (PR-030). Created `docs/0.3/{DESIGN-log, ROADMAP, RESEARCH-BACKLOG}.md` + 7 PR stubs (`prs/PR-030..PR-036`) sketching the v0.3 "honesty cut" — completes 4 phantom implementations surfaced by the 2026-05-20 audit: (1) holdout test fold never consumed → PR-031 + PR-032; (2) ExtMem out-of-core ingest path wired in design but never executed → PR-033 (covers borderline phantom 2b: `select_ingest` drives telemetry only); (3) Optuna `FileSystemArtifactStore` declared but unused → PR-034; (4) `rux-ml tune retry-trial` verb body real but zero test coverage → PR-035. PR-036 is the v0.3.0 version-cut ritual. Architectural sub-decisions (D1–D4) deferred to a v0.3 design session that runs after PR-030 merges, before PR-031 implementation begins. No source / test / config changes — sprint scaffolding only.

### Changed
- `PROCEDURE-pr-research.md` syncs stack-neutral cleanup from the `vibe-rails` template (commit `fd0e6dd`, on top of `fb0eec9` that PR-028 mirrored). Removes 7 stack-flavored sites — the Cite-or-flag clause's "action input / multi-step workflows / action+input pairings" parentheticals (now "configuration input / multi-step procedures / parameter+value pairings"); the Group D Scope filter's "vendor-specific" ("implementation-specific"); Probe 1's 5-bullet stack-list (webhook payloads / `.d.ts` / OpenAPI / `action.yaml` / CLI `--help`) collapses to an abstract statement of the rule; Probe 2 terminology touch-ups; Probe 3's GitHub-deployment-flavored examples (deployment checks / commit-status contexts / webhook subscriptions / `gh api`) become generic lifecycle-binding examples (factory registrations / hook subscriptions / external-system bindings / indexer entries / persisted configuration); Group D output table example values become generic placeholders. Rux-ml's 4 versioned-path lines (`docs/0.2/...`) stay preserved per the hybrid layout convention. **First PR to operationally exercise the new procedure** (Phase 1 prior-art audit + Group D Probes 1+2 verified; Probe 3 N/A by Scope filter — confirms the "Probe 3 N/A for non-vendor-binding PRs" operational pattern). No source / test / config changes — procedural refactor only (PR-029).
- `PROCEDURE-pr-research.md` syncs three additions from the `vibe-rails` template (commit `fb0eec9`, itself propagated from `Casus PR-032`): (1) Phase 1 gains a **prior-art audit step** — for every file the PR will touch, run `git log -p` and skim per-commit diffs to surface previously-validated patterns that constrain Phase 3 (so research doesn't re-derive them) and Phase 4 (so synthesis doesn't drop them); (2) Phase 3 Rules gain a non-negotiable **cite-or-flag clause** — every identifier in a recommendation needs a source-of-truth cite, every combination needs a cited working example using the EXACT combination, missing either → `best-guess-given-constraints` + flag (rationale: synthesis from disparate sources is a more pernicious agent-research failure mode than outright fabrication); (3) Phase 3 gains a **Group D MCP-Verification Round** (mandatory before locking Phase 4, ≤30 min, bounded) with three named probes — Schema-Integrity (verify named identifiers exist in the canonical schema documenter), Synthesis-Verification (verify combinations have a cited working example), Binding-at-creation (for vendor-side bindings, confirm registration occurs at the expected lifecycle moment via MCP introspection). Rux-ml's 4 versioned-path lines (`docs/0.2/...` instead of bare `docs/...`) are preserved per the hybrid layout convention. No source / test / config changes — procedural refactor only (PR-028).

## [0.2.0] - 2026-05-19

### Fixed
- **`TimeSeriesSplitCV.embargo_time="<duration>"` on `pl.Int64` `time_column` no longer mis-interprets the integers as nanoseconds-since-epoch.** Pre-PR-027 (PR-023 D2 implementation), polars' `cast(pl.Datetime("ns"))` on an `Int64` column reinterpreted the integers AS ns with no unit conversion — a Unix-seconds delta of 86400 stayed 86400 (treated as 86.4 μs), driving `_effective_gap` to ~1e11 rows and crashing sklearn `TimeSeriesSplit` with `Too many splits`. PR-027 adds a `time_unit: Literal["ns","us","ms","s"] | None = None` field on `TimeSeriesSplitCV`: `pl.Int64` columns require it (raise `ValueError` otherwise); `pl.Datetime` columns ignore it (raise `ValueError` if set, signaling user confusion). Convention inherited from polars `pl.from_epoch(time_unit=...)` and Nixtla's `validate_freq` pattern (4-of-6 surveyed CV / time-series libraries enforce explicit unit declaration; see PR-027 Phase 3). The crypto-h3 panel currently uses `PanelCombinatorialPurgedCV` which sidesteps this code path, so no committed config was affected — but anyone configuring `TimeSeriesSplitCV(embargo_time="<duration>", time_column="<pl.Int64 column>")` would have hit a sklearn `ValueError`. Four new tests under `tests/data/test_panel_cv.py` cover the Int64 path + Datetime guard rails (PR-027).
- **Stacked-panel CV no longer silently under-embargoes per-asset forward labels.** `TimeSeriesSplitCV.gap` is row-count; on a K-row-per-timestamp panel that collapsed to ≈ `gap / K` timestamps of per-asset separation (1084-asset hourly panel: `gap=24` ≈ 0.022 timestamps, not 24 hours). PR-023 surfaces two panel-aware paths — `TimeSeriesSplitCV.embargo_time="<duration>"` (D2; e.g. `"24h"`) and the new `PanelCombinatorialPurgedCV` variant (D1) that folds over unique sorted timestamps so per-asset purge is timestamp-atomic. Pre-existing single-asset users are unaffected; defaults preserve v0.1.1 row-count behavior. Live finding from the 2026-05-18 first-real-dataset run; research-backed by sktime / Nixtla / Darts (polymorphic time-units convention) + mlfinlab `StackedCombinatorialPurgedKFold` + Numerai era-CV (panel-CPCV convention).

### Added
- **`PanelCombinatorialPurgedCV` Splitter variant** (PR-023 D1). New `[cv] kind = "panel_cpcv"` variant: folds are computed over the **unique sorted timestamps** read from `time_column`; skfolio CPCV runs on the timestamp axis (so `target_horizon_bars` and `embargo_pct` are interpreted in timestamp units); each timestamp-level fold maps back to row indices, making per-asset purge timestamp-atomic. `asset_column` is required and validated at split time. Convention: mlfinlab `StackedCombinatorialPurgedKFold` + Numerai era-CV.
- **Ergonomic AFML knobs on `CombinatorialPurgedCV`** (PR-023 D3). New fields `target_horizon_bars: int = 0` + `embargo_pct: float = 0.0`; the splitter converts internally to skfolio's row-count `purged_size` / `embargo_size = int(N * embargo_pct)` per AFML Snippet 7.3. When both ergonomic and row-count siblings are non-zero, the row-count value wins (strict opt-in). Defaults preserve v0.1.1 behavior.
- **`docs/ARCHITECTURE.md` Decision Rules**: new entry for **CV strategy by data shape** (single-asset vs stacked panel; row-count vs time-unit embargo) per PR-023 D1/D2 (refines the existing per-strategy default table to flag the panel foot-gun explicitly).
- **`docs/CONVENTIONS.md` CV strategy conventions**: panel row added to the CV-default-by-data-shape table; new `TimeSeriesSplitCV.embargo_time` polymorphic semantics subsection; new `PanelCombinatorialPurgedCV` subsection; mandatory **skfolio purge precision gap** note (PR-023 D4) documenting that skfolio's scalar two-sided `purged_size` is conservative-correct (drops more than AFML's interval-overlap; never leaks) and pointing users to mlfinlab/timeseriescv for interval-overlap precision.
- `tests/data/test_panel_cv.py` — 20 new tests across three layers: hand-verified index fixtures on tiny synthetic panels (3 assets × 9 timestamps); property tests (pairwise-disjoint train/test, per-asset embargo loop, timestamp atomicity, n_splits combinatorial); shuffle-null tripwire (no-skill baseline residual ≈ y_var) for stacked-panel CV (PR-023 D5).
- **`temporal_train_val_test_split` + `data.split_kind` dispatch** (PR-024). New function `rux_ml.data.splits.temporal_train_val_test_split(df, *, time_column, ratios)` sorts by `time_column` and slices into time-ordered partitions (deterministic; no seed). New fields on `DataConfig`: `split_kind: Literal["random", "time_ordered"] = "random"` (default preserves v0.1.1 random-shuffle behavior) + `time_column: str | None = None`. New helper `make_splits(cfg, df, *, seed)` is the dispatcher consumed by `rux-ml train` and `registry/promote.py` — same call site for both branches keeps the cli/train ↔ registry/promote reproducibility contract automatic. New cross-field model_validator on `RuxMLConfig` enforces two rules at config-load (fail-fast `ValueError`): (a) `data.split_kind == "time_ordered"` requires `data.time_column` to be set; (b) `cv.kind ∈ {"time_series", "cpcv", "panel_cpcv"}` requires `data.split_kind == "time_ordered"`. Convention: ≥3 cited mature systems (sktime `temporal_train_test_split`, Darts `TimeSeries.split_before/split_after`, AutoGluon TimeSeriesPredictor, Nixtla `mlforecast.cross_validation`, mlfinlab) all use two separate functions, not a kind-knob; quantified leakage evidence: arxiv 2512.06932 (Dec 2025) reports 19–21% RMSE inflation under leaky splits.
- `docs/ARCHITECTURE.md` Decision Rules — CV-strategy-by-data-shape entry gains a one-off-baseline-path paragraph cross-referencing `data.split_kind` (PR-024).
- `docs/CONVENTIONS.md` CV-strategy conventions — one-shot subsection now lists both `train_val_test_split` and `temporal_train_val_test_split` + documents the `data.split_kind` × `cv.kind` consistency validator (PR-024).
- 16 new tests for PR-024: `tests/data/test_splits.py` gains 13 (temporal split determinism / ordering / panel structure / missing-column / empty-frame; dispatcher routing; validator rules); `tests/cli/test_train_subcommand.py` gains 3 CLI integration tests (`time_ordered` end-to-end through the dispatcher; validator rejects missing `time_column`; validator rejects temporal CV with random split).

### Changed
- **`TimeSeriesSplitCV` polymorphic embargo** (PR-023 D2). New fields `time_column: str | None = None` + `embargo_time: int | str | None = None`. When `embargo_time` is set it wins over `gap`: an `int` is row-count (matches `gap` semantics); a duration string like `"24h"` requires `time_column` and is parsed via `pandas.Timedelta`, then translated to a row-count gap by computing the median delta between unique sorted timestamps and the mean rows-per-unique-timestamp. Exact on regular bars; conservative best-guess on irregular bars (out-of-scope precision work tracked in `docs/0.2/RESEARCH-BACKLOG.md` A2). Defaults preserve v0.1.1 behavior. Convention: sktime `SlidingWindowSplitter`, Nixtla `mlforecast.cross_validation`, Darts `historical_forecasts`.
- **`rux-ml train` + `registry/promote.py` route through `make_splits` dispatcher** (PR-024). Both call sites previously called `train_val_test_split` directly; now they call `make_splits(cfg, df, seed=bag.split_seed)` which dispatches on `cfg.data.split_kind`. The reproducibility contract (`cli/train` ↔ `registry/promote` same split for same trial) is preserved by construction — the random path threads the same seed; the temporal path is deterministic.
- **`docs/CONVENTIONS.md` + `docs/ARCHITECTURE.md` HPO objective sections quantify the Position A optimism bias** (PR-025). Calibrated A/B/C on the crypto-h3 panel-CPCV configuration (10 Optuna trials each, same splits + seeds): **A median RMSE 0.027101, B median 0.027108 (identical to A at 4 decimals; diff = 7e-06, well within Optuna search noise), C median 0.027215 (+0.42% vs A; +65% slower wallclock).** B wallclock is **−29% vs A** (B is the fastest position; it skips one XGBoost evaluation per fold because the test fold is not also used for early stopping). Position A's "optimism bias" did not materialize empirically on this dataset — A and B are operationally indistinguishable. **Workbench stays on Position A**; the existing optimism-bias warning still stands as a per-problem caveat. Switching to Shape 2 (Position B) was ruled out as no-benefit + high-cost (`_fold_scores` rewrite + per-family eval_set adapters across XGBoost / LightGBM Pattern-A shim / CatBoost). Calibration receipt + per-trial timings + memory peaks committed at `prs/PR-025-calibration-results.json`; harness at `scripts/calibrate_pr025.py` (research artifact — not a runtime path).

## [0.1.1] - 2026-05-18

### Fixed
- `configs/base.toml [cv]` no longer carries variant-specific defaults. `shuffle = true` (a `KFoldCV`-only field) and `n_splits = 5` (absent from `CombinatorialPurgedCV`, which uses `n_folds`) leaked across the discriminator on `deep_merge` and tripped Pydantic's `extra="forbid"` whenever a problem TOML switched `kind` to `time_series` / `group_kfold` / `cpcv`. Both fields are now schema-defaults (preserving identical behavior for kfold users). New `tests/config/test_base_toml_discriminator_hygiene.py` walks every top-level discriminated union (`[cv]`, `[training]`, `[solving]`) and asserts no variant-specific field lives at the base-table position — future regressions of the same shape fail CI, not the user at runtime (PR-022).
- `docs/CONVENTIONS.md` HPO objective section + `docs/ARCHITECTURE.md` HPO components section now honestly characterize the test-fold-as-eval_set design as a research-acknowledged pragmatic deviation from textbook CV (Position A of four practitioner positions), not as a clean pattern inherited from Optuna's WilcoxonPruner recipe. The HPO bias compounding risk is now documented, and the two alternatives (Position B — inner val from train fold, sklearn's `HistGradientBoosting*` default; Position C — no early stopping in CV with post-CV retrain, XGBoost's own recommendation) are noted as explicitly available, deferred to a future Tier-2 study (PR-022 Phase 3 research, 2026-05-18).

### Added
- `docs/CONVENTIONS.md` Configuration Conventions: new subsection on **base.toml discriminator-table hygiene** — variant-specific knobs go in `configs/problems/<n>.toml`, not base. Enforced by the new structural test. Documents `kind` vs `type` discriminator field distinction (PR-022).
- `docs/CONVENTIONS.md` CV strategy conventions: new subsection on **`TimeSeriesSplitCV.gap` row-vs-time semantics** — `gap` is row-count not time-units; on stacked panels this collapses to <1 hour of per-asset embargo when many assets share each timestamp. Forward-references PR-023 (time-aware embargo for panel data). Live finding from the 2026-05-18 first-real-dataset run (PR-022).

## [0.1.0] - 2026-05-18

### Added
- LightGBM Trainer family. Install via `uv sync --extra lightgbm`. Selected per problem via `[training]` `kind = "lightgbm"`. CPU-only in v0.1 (per Q-GPU research: LightGBM-GPU is 8-28x slower than XGBoost-GPU on workbench-scale data; install path via uv is non-trivial). Categorical features integrate cleanly with the existing `_ColumnRouter` (no pipeline changes); reference HPO search-space template at `configs/search_spaces/lightgbm.toml` (PR-018).
- CatBoost Trainer family. Install via `uv sync --extra catboost`. Selected per problem via `[training]` `kind = "catboost"`. **GPU is the default** — CatBoost ships prebuilt CUDA wheels via `uv add catboost`, no extras / source build / container delta needed (RTX 4090 field-confirmed). `cat_features=` is auto-extracted from the DataFrame at fit time by `_CatBoostTrainerShim` (CatBoost does NOT auto-detect pandas Categorical dtype unlike LightGBM). Uses Intel TBB threading, NOT OpenMP — factory reads `OMP_NUM_THREADS` and passes explicitly as `thread_count=`. Reference HPO search-space template at `configs/search_spaces/catboost.toml` with 7 search dims (PR-019).
- **Solver layer**. Parallel top-level surface to the Trainer layer per `docs/0.1/DESIGN-log.md` Q1 (two parallel `typing.Protocol`s, no shared parent). <!-- rewrite-doc-refs:skip-line --> New `src/rux_ml/solving/` subpackage + `SOLVER_FAMILIES` registry + `make_solver(cfg)` factory + `Solver.solve(problem) → SolverResult`. First family: **CVXPY** (zero dep addition — cvxpy + clarabel were already transitive via skfolio; promoted to direct deps). `cfg.solving.solver` selects a backend (`CLARABEL` default; `OSQP` / `SCS` / `ECOS` / `HIGHS` also routable through cvxpy). `cfg.solving.solver_opts: dict[str, Any]` is the escape hatch for backend-specific tolerances/iter knobs. New CLI verb: `rux-ml solve --problem <name>` runs a single solve (analog of `rux-ml train`; solver-internal HPO deferred to follow-up). Per Q-Shape research = partial-mirror integration: solver runs reuse Optuna study + per-trial provenance (`TrialAttrs` extended with Optional `solving_cfg_hash` + `solver_status` + `objective_value` + `solver_iter_count` + `solve_time_s`) but bypass `cfg.data` / `cfg.cv` / fittable-model registry (PR-020).

### Changed
- Adopt vibe-rails hybrid docs-versioning layout: temporal docs (`DESIGN-log`, `RESEARCH-BACKLOG`, `ROADMAP`) move under `docs/0.0/`; flat SSOT files (`ARCHITECTURE`, `CONSTRAINTS`, `CONVENTIONS`) stay at `docs/`. Add `docs/VERSIONING.md`, `docs/DEPLOYMENT.md`, `/CHANGELOG.md`. Add `Landed-in:` header to every numbered PR file. Add `scripts/rewrite_doc_refs.py` migrator + `make rewrite-doc-refs` targets. Append Per-Phase Approval Gate (NON-NEGOTIABLE) to `docs/CONSTRAINTS.md` — Claude halts at every phase boundary of every multi-phase procedure (PR-016).
- Trainer registry refactor: `src/rux_ml/training/` is now multi-family. XGBoost integration moved into `src/rux_ml/training/xgboost/` subpackage. `TrainingConfig` is a Pydantic v2 discriminated union over per-family variants with `kind` discriminator. `TRAINER_FAMILIES` registry dict (B-explicit, HF transformers `MODEL_MAPPING_NAMES` pattern). New parametrized conformance test `tests/training/test_registry_conformance.py` is the stale-path tripwire (PR-017).
- Sentinel `PASSTHROUGH_TO_XGB_CATEGORICAL` renamed to `PASSTHROUGH_NATIVE_CATEGORICAL` — same sentinel now serves XGBoost AND LightGBM AND future CatBoost (PR-018 Q-Cat).
- v0.0 → v0.1 docs-version cut: `scripts/rewrite_doc_refs.py` PATH_REWRITES updated for `docs/0.0/* → docs/0.1/*` migration; added per-file `OPT_OUT_GLOBS` (skips `prs/`, `docs/0.0/`, `docs/0.1/`, `src/`, `tests/`) and per-line `<!-- rewrite-doc-refs:skip-line -->` sentinel for locking historical citations (e.g., "D1–D17 in DESIGN-log.md") against re-runs. Idempotency contract preserved (PR-021).

## [0.0.1] - 2026-05-16

### Added
- Initial v0 workbench: end-to-end **data → features → training → tuning → run logging → registry promotion** lifecycle. Single CLI (`rux-ml`) over the Python package (`rux_ml`). XGBoost-first via GPU (`device="cuda"`); three-tier TOML config (`base.toml` → `problems/<n>.toml` → `studies/<n>.toml`) with `CLI > env > .env > study > problem > base` override precedence. Optuna sequential trials with subprocess-per-trial spawn isolation, SQLite study, K-fold CV-mean objective, WilcoxonPruner, TPE sampler. Five `Splitter` strategies (`KFold` / `StratifiedKFold` / `TimeSeriesSplit` / `GroupKFold` / `CombinatorialPurgedCV`). Two-file model bundle (`pipeline.skops` + `model.ubj` + Pydantic-validated `manifest.json`); atomic `champion.json` rewrite. Per-trial provenance triple in `user_attrs` (`data_hash`, per-layer/root config hashes, `git_sha`, `entropy_hex`, `image_digest`, library/CUDA/driver versions, `omp_threads`, `peak_rss_mb`). Memory watchdog with `MemoryPressureError` → `optuna.TrialPruned`. Tolerance-based golden regression tests (CPU bit-exact contract). Container image with NVIDIA CUDA 12.4.1 base, GPU passthrough via Compose, `mem_limit: 32g`, image-digest pinning (PR-001 through PR-015).

[Unreleased]: https://github.com/rux-eth/rux-ml/compare/v0.2.0...HEAD
[0.2.0]: https://github.com/rux-eth/rux-ml/compare/v0.1.1...v0.2.0
[0.1.1]: https://github.com/rux-eth/rux-ml/compare/v0.1.0...v0.1.1
[0.1.0]: https://github.com/rux-eth/rux-ml/compare/v0.0.1...v0.1.0
[0.0.1]: https://github.com/rux-eth/rux-ml/releases/tag/v0.0.1
